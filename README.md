# AI-FRAUD-DETECTION
AI Fraud Detection System is a machine learning project that detects fraudulent credit card transactions. It uses Python, data preprocessing, normalization, feature selection, and class-imbalance handling with Logistic Regression, Random Forest, and XGBoost. Models are evaluated using Accuracy, Precision, Recall, F1-Score, and ROC-AUC.
